== README

Devise login for multiple models.
I've created three models:
* User
* Trainer
* Admin
where all three of them have their own login/sign up URL, where, after a succesfull login, you're redirected to a specific root page for each of the model.


* Ruby version

* System dependencies
